# mods/ holds user mods. The plugin scaffolds an example mod here on
# first run if the directory is missing; once it exists, contents are
# the operator's domain — not part of the plugin source.
mods/

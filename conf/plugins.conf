# rathena Plugin Configuration
# List the plugins to load, one per line.
# Paths are relative to the server working directory.
# The platform extension (.dll / .so) is added automatically if omitted.
#
# Example:
#   plugins/example/example_plugin
#   plugins/my_custom/my_custom
#
# Lines starting with # or // are comments.

# plugins/example/example_plugin
